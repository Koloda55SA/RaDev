# 🚀 Быстрый запуск бэкенда на VPS

## Проверка существующей конфигурации

```bash
# Запустите скрипт проверки
./check-and-start-vps.sh
```

Этот скрипт покажет:
- Где находится docker-compose.yml
- Какие контейнеры запущены
- Структуру директорий
- Логи контейнеров

## Запуск бэкенда

```bash
# Запустите скрипт запуска
./start-vps-backend.sh
```

Скрипт:
1. Найдет существующий docker-compose.yml
2. Остановит старые контейнеры
3. Запустит контейнеры заново
4. Покажет статус и логи

## Ручной запуск (если скрипты не работают)

```bash
# Подключитесь к VPS
ssh root@213.199.56.27

# Найдите docker-compose.yml
find ~ -name "docker-compose.yml"
find /opt -name "docker-compose.yml"

# Перейдите в директорию с docker-compose.yml
cd ~/FreeDip/backend  # или /opt/freedip-backend/backend

# Запустите
docker-compose up -d

# Проверьте логи
docker-compose logs -f api
```

## Проверка работы

```bash
# На VPS
curl http://localhost:5000/health

# С вашего компьютера
curl http://213.199.56.27:5000/health
```

## Если API не запускается

```bash
# Проверьте логи
docker-compose logs api

# Проверьте статус контейнеров
docker-compose ps

# Проверьте порты
netstat -tulpn | grep 5000
```


