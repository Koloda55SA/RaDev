# 🔐 Архитектура: Firebase Auth + C# Backend

## 📋 Текущая архитектура

### ✅ Аутентификация
- **Firebase Authentication** - для входа, регистрации, Google OAuth
- Все методы аутентификации (`signInWithEmail`, `signUpWithEmail`, `signInWithGoogle`) работают через Firebase

### ✅ Остальные функции
- **C# Backend API** - для всех остальных функций:
  - 💬 Чат (глобальный и приватный)
  - 👤 Профили пользователей
  - 📁 Загрузка файлов (аватары, файлы в чате)
  - 📚 Курсы и прогресс
  - 🔔 Социальные функции (лайки, подписки, поиск)
  - 📊 Статистика и достижения

---

## 🔑 Как работает авторизация

### 1. Пользователь входит через Firebase
```typescript
// Пользователь входит через Firebase
const { signInWithEmail } = useAuth() // Firebase AuthProvider
await signInWithEmail(email, password)
```

### 2. Firebase токен используется для запросов к C# API
```typescript
// API Client автоматически получает Firebase токен
const response = await apiClient.sendGlobalMessage('Привет!')
// Внутри apiClient:
// 1. Получает Firebase токен: auth.currentUser.getIdToken()
// 2. Отправляет запрос с заголовком: Authorization: Bearer <firebase_token>
```

### 3. C# Backend проверяет Firebase токен
```csharp
// C# Backend должен проверить Firebase токен
// Можно использовать Firebase Admin SDK для проверки токена
```

---

## 📝 Структура файлов

### Аутентификация (Firebase)
- `components/auth/AuthProvider.tsx` - Firebase провайдер аутентификации
- `components/auth/HybridAuthProvider.tsx` - Всегда использует Firebase для auth
- `lib/firebase/config.ts` - Конфигурация Firebase

### API Client (C# Backend)
- `lib/api/config.ts` - Конфигурация C# API
- `lib/api/client.ts` - API клиент (автоматически получает Firebase токен)
- `components/auth/useAuth.ts` - Единый хук useAuth (работает с Firebase)

---

## 🔧 Настройка

### 1. Firebase (уже настроен)
- ✅ Firebase Authentication включен
- ✅ Google OAuth настроен
- ✅ Firestore и Storage могут использоваться для других целей

### 2. C# Backend
- ✅ Должен принимать Firebase токены в заголовке `Authorization: Bearer <token>`
- ✅ Должен проверять токен через Firebase Admin SDK
- ✅ Должен извлекать `uid` и `email` из токена

### 3. Переменные окружения

#### Frontend (Vercel):
```env
NEXT_PUBLIC_API_URL=https://your-vps-domain.com/api
NEXT_PUBLIC_USE_API_BACKEND=true
# Firebase настройки уже есть
```

#### C# Backend (VPS):
```env
# Firebase Admin SDK для проверки токенов
FIREBASE_PROJECT_ID=freedip-27d92
FIREBASE_PRIVATE_KEY=...
FIREBASE_CLIENT_EMAIL=...
```

---

## 💻 Примеры использования

### Отправка сообщения в чат
```typescript
import { useAuth } from '@/components/auth/useAuth'
import { apiClient } from '@/lib/api/client'

function ChatComponent() {
  const { user } = useAuth() // Firebase user
  
  const sendMessage = async () => {
    // API Client автоматически получит Firebase токен
    const response = await apiClient.sendGlobalMessage('Привет!')
    if (response.success) {
      console.log('Сообщение отправлено!')
    }
  }
}
```

### Загрузка аватара
```typescript
import { apiClient } from '@/lib/api/client'

const uploadAvatar = async (file: File) => {
  // API Client автоматически получит Firebase токен
  const response = await apiClient.uploadAvatar(file)
  if (response.success && response.data) {
    console.log('Аватар загружен:', response.data.url)
  }
}
```

### Получение профиля
```typescript
import { apiClient } from '@/lib/api/client'

const getProfile = async () => {
  // API Client автоматически получит Firebase токен
  const response = await apiClient.getCurrentUser()
  if (response.success && response.data) {
    console.log('Профиль:', response.data)
  }
}
```

---

## 🔐 Безопасность

### Firebase токены
- ✅ Токены автоматически обновляются
- ✅ Токены имеют срок действия (1 час)
- ✅ API Client автоматически получает новый токен при необходимости

### C# Backend проверка
- ✅ Должен проверять каждый запрос с Firebase токеном
- ✅ Должен извлекать `uid` из токена для идентификации пользователя
- ✅ Не должен доверять `uid` из тела запроса, только из токена

---

## 📊 Преимущества этой архитектуры

1. ✅ **Простота**: Firebase Auth уже настроен и работает
2. ✅ **Безопасность**: Firebase токены надежны и проверены
3. ✅ **Гибкость**: C# Backend может использовать любые данные из токена
4. ✅ **Масштабируемость**: Легко добавить новые функции в C# Backend
5. ✅ **Миграция**: Постепенная миграция функций на C# Backend

---

## 🚀 Следующие шаги

1. ✅ Аутентификация через Firebase (готово)
2. ⏳ Настроить C# Backend для проверки Firebase токенов
3. ⏳ Заменить чат на C# API
4. ⏳ Заменить профили на C# API
5. ⏳ Заменить загрузку файлов на C# API

---

**Готово!** Теперь аутентификация через Firebase, все остальное через C# Backend! 🎉


## 📋 Текущая архитектура

### ✅ Аутентификация
- **Firebase Authentication** - для входа, регистрации, Google OAuth
- Все методы аутентификации (`signInWithEmail`, `signUpWithEmail`, `signInWithGoogle`) работают через Firebase

### ✅ Остальные функции
- **C# Backend API** - для всех остальных функций:
  - 💬 Чат (глобальный и приватный)
  - 👤 Профили пользователей
  - 📁 Загрузка файлов (аватары, файлы в чате)
  - 📚 Курсы и прогресс
  - 🔔 Социальные функции (лайки, подписки, поиск)
  - 📊 Статистика и достижения

---

## 🔑 Как работает авторизация

### 1. Пользователь входит через Firebase
```typescript
// Пользователь входит через Firebase
const { signInWithEmail } = useAuth() // Firebase AuthProvider
await signInWithEmail(email, password)
```

### 2. Firebase токен используется для запросов к C# API
```typescript
// API Client автоматически получает Firebase токен
const response = await apiClient.sendGlobalMessage('Привет!')
// Внутри apiClient:
// 1. Получает Firebase токен: auth.currentUser.getIdToken()
// 2. Отправляет запрос с заголовком: Authorization: Bearer <firebase_token>
```

### 3. C# Backend проверяет Firebase токен
```csharp
// C# Backend должен проверить Firebase токен
// Можно использовать Firebase Admin SDK для проверки токена
```

---

## 📝 Структура файлов

### Аутентификация (Firebase)
- `components/auth/AuthProvider.tsx` - Firebase провайдер аутентификации
- `components/auth/HybridAuthProvider.tsx` - Всегда использует Firebase для auth
- `lib/firebase/config.ts` - Конфигурация Firebase

### API Client (C# Backend)
- `lib/api/config.ts` - Конфигурация C# API
- `lib/api/client.ts` - API клиент (автоматически получает Firebase токен)
- `components/auth/useAuth.ts` - Единый хук useAuth (работает с Firebase)

---

## 🔧 Настройка

### 1. Firebase (уже настроен)
- ✅ Firebase Authentication включен
- ✅ Google OAuth настроен
- ✅ Firestore и Storage могут использоваться для других целей

### 2. C# Backend
- ✅ Должен принимать Firebase токены в заголовке `Authorization: Bearer <token>`
- ✅ Должен проверять токен через Firebase Admin SDK
- ✅ Должен извлекать `uid` и `email` из токена

### 3. Переменные окружения

#### Frontend (Vercel):
```env
NEXT_PUBLIC_API_URL=https://your-vps-domain.com/api
NEXT_PUBLIC_USE_API_BACKEND=true
# Firebase настройки уже есть
```

#### C# Backend (VPS):
```env
# Firebase Admin SDK для проверки токенов
FIREBASE_PROJECT_ID=freedip-27d92
FIREBASE_PRIVATE_KEY=...
FIREBASE_CLIENT_EMAIL=...
```

---

## 💻 Примеры использования

### Отправка сообщения в чат
```typescript
import { useAuth } from '@/components/auth/useAuth'
import { apiClient } from '@/lib/api/client'

function ChatComponent() {
  const { user } = useAuth() // Firebase user
  
  const sendMessage = async () => {
    // API Client автоматически получит Firebase токен
    const response = await apiClient.sendGlobalMessage('Привет!')
    if (response.success) {
      console.log('Сообщение отправлено!')
    }
  }
}
```

### Загрузка аватара
```typescript
import { apiClient } from '@/lib/api/client'

const uploadAvatar = async (file: File) => {
  // API Client автоматически получит Firebase токен
  const response = await apiClient.uploadAvatar(file)
  if (response.success && response.data) {
    console.log('Аватар загружен:', response.data.url)
  }
}
```

### Получение профиля
```typescript
import { apiClient } from '@/lib/api/client'

const getProfile = async () => {
  // API Client автоматически получит Firebase токен
  const response = await apiClient.getCurrentUser()
  if (response.success && response.data) {
    console.log('Профиль:', response.data)
  }
}
```

---

## 🔐 Безопасность

### Firebase токены
- ✅ Токены автоматически обновляются
- ✅ Токены имеют срок действия (1 час)
- ✅ API Client автоматически получает новый токен при необходимости

### C# Backend проверка
- ✅ Должен проверять каждый запрос с Firebase токеном
- ✅ Должен извлекать `uid` из токена для идентификации пользователя
- ✅ Не должен доверять `uid` из тела запроса, только из токена

---

## 📊 Преимущества этой архитектуры

1. ✅ **Простота**: Firebase Auth уже настроен и работает
2. ✅ **Безопасность**: Firebase токены надежны и проверены
3. ✅ **Гибкость**: C# Backend может использовать любые данные из токена
4. ✅ **Масштабируемость**: Легко добавить новые функции в C# Backend
5. ✅ **Миграция**: Постепенная миграция функций на C# Backend

---

## 🚀 Следующие шаги

1. ✅ Аутентификация через Firebase (готово)
2. ⏳ Настроить C# Backend для проверки Firebase токенов
3. ⏳ Заменить чат на C# API
4. ⏳ Заменить профили на C# API
5. ⏳ Заменить загрузку файлов на C# API

---

**Готово!** Теперь аутентификация через Firebase, все остальное через C# Backend! 🎉




