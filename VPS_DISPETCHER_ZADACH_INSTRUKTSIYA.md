# 🖥️ Инструкция по диспетчеру задач VPS

## 📋 Где находится диспетчер задач на VPS

Диспетчер задач на VPS (Linux сервере) называется **системным монитором** или **task manager**. В отличие от Windows, здесь нет графического интерфейса по умолчанию, но есть несколько способов мониторинга системы.

---

## 🔧 Способ 1: Командная строка (htop/top)

### Установка htop (рекомендуется)
```bash
# Для Ubuntu/Debian
sudo apt update
sudo apt install htop -y

# Для CentOS/RHEL
sudo yum install htop -y
```

### Использование htop
```bash
htop
```

**Горячие клавиши:**
- `F2` - Настройки
- `F3` - Поиск процесса
- `F4` - Фильтр
- `F5` - Дерево процессов
- `F6` - Сортировка
- `F9` - Убить процесс
- `F10` - Выход
- `q` - Выход

### Использование top (встроенный)
```bash
top
```

**Горячие клавиши:**
- `q` - Выход
- `k` - Убить процесс (потребует PID)
- `M` - Сортировка по памяти
- `P` - Сортировка по CPU
- `1` - Показать все CPU ядра

---

## 🎨 Способ 2: Веб-интерфейс (рекомендуется для вас)

Я создал веб-интерфейс для мониторинга VPS, который доступен по адресу:

### Локальный доступ
```
http://localhost:3000/admin/monitor
```

### Удаленный доступ (если настроен)
```
https://your-domain.com/admin/monitor
```

### Что показывает интерфейс:
- 📊 **CPU Usage** - Использование процессора (%)
- 💾 **Memory Usage** - Использование памяти (MB/GB)
- 💿 **Disk Usage** - Использование диска (GB)
- 🌐 **Network** - Сетевая активность (входящий/исходящий трафик)
- 🔄 **Uptime** - Время работы сервера
- 📈 **Load Average** - Средняя нагрузка системы

---

## 🛠️ Способ 3: Установка веб-интерфейса (Netdata/Grafana)

### Netdata (простой и красивый)
```bash
# Установка Netdata
bash <(curl -Ss https://my-netdata.io/kickstart.sh)

# После установки доступен по адресу:
# http://your-vps-ip:19999
```

### Grafana + Prometheus (продвинутый)
```bash
# Установка через Docker Compose
cd ~/FreeDip/backend
# Добавьте в docker-compose.yml сервисы для мониторинга
```

---

## 📊 Способ 4: Полезные команды для мониторинга

### Просмотр процессов
```bash
# Все процессы
ps aux

# Процессы с фильтром
ps aux | grep nginx
ps aux | grep dotnet
ps aux | grep postgres
```

### Использование памяти
```bash
# Краткая информация
free -h

# Детальная информация
cat /proc/meminfo
```

### Использование диска
```bash
# Использование дисков
df -h

# Размер папок
du -sh /var/*
du -sh ~/FreeDip/*
```

### Использование CPU
```bash
# Информация о CPU
lscpu

# Нагрузка
uptime
```

### Сетевые соединения
```bash
# Активные соединения
netstat -tulpn

# Или
ss -tulpn
```

### Логи системы
```bash
# Системные логи
journalctl -f

# Логи Docker
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f api
docker-compose logs -f postgres
```

---

## 🎯 Где находится веб-интерфейс мониторинга

### В проекте FreeDip

Файл: `app/admin/monitor/page.tsx`

**Как открыть:**
1. Войдите в систему как администратор
2. Перейдите по адресу: `/admin/monitor`
3. Или через меню: **Админка** → **Мониторинг**

### API для получения статистики

Файл: `app/api/admin/system-stats/route.ts`

**Эндпоинт:** `GET /api/admin/system-stats`

**Ответ:**
```json
{
  "cpu": {
    "usage": 25.5,
    "cores": 4
  },
  "memory": {
    "used": 2048,
    "total": 4096,
    "percentage": 50
  },
  "disk": {
    "used": 50,
    "total": 100,
    "percentage": 50
  },
  "network": {
    "incoming": 1024,
    "outgoing": 512
  },
  "uptime": 86400
}
```

---

## 🔐 Настройка доступа к мониторингу

### Требования:
1. Пользователь должен быть авторизован
2. Пользователь должен иметь роль `admin`

### Проверка роли:
В `app/admin/monitor/page.tsx` есть проверка:
```typescript
const { user, userRole } = useAuth()

if (userRole !== 'admin') {
  return <div>Доступ запрещен</div>
}
```

---

## 📱 Мобильная версия

Веб-интерфейс мониторинга адаптивен и работает на мобильных устройствах.

---

## 🚀 Автоматическое обновление данных

Данные обновляются каждые 5 секунд автоматически через `setInterval`.

---

## 🛡️ Безопасность

⚠️ **ВАЖНО**: 
- Мониторинг доступен только администраторам
- Не публикуйте URL мониторинга публично
- Используйте HTTPS для production
- Ограничьте доступ по IP если возможно

---

## 📝 Полезные команды для быстрого мониторинга

```bash
# Быстрый обзор системы
htop

# Использование ресурсов Docker
docker stats

# Логи в реальном времени
docker-compose logs -f

# Проверка места на диске
df -h

# Проверка памяти
free -h

# Проверка нагрузки
uptime
```

---

## 🎨 Кастомизация интерфейса

Вы можете изменить интерфейс мониторинга, отредактировав:
- `app/admin/monitor/page.tsx` - основной компонент
- `app/api/admin/system-stats/route.ts` - API для получения данных

---

## 🔗 Дополнительные инструменты

### Glances (альтернатива htop)
```bash
pip install glances
glances
```

### Btop (современная альтернатива)
```bash
# Установка
sudo apt install btop -y
# Использование
btop
```

---

**Готово!** Теперь вы знаете, где находится диспетчер задач на VPS и как им пользоваться! 🎉


## 📋 Где находится диспетчер задач на VPS

Диспетчер задач на VPS (Linux сервере) называется **системным монитором** или **task manager**. В отличие от Windows, здесь нет графического интерфейса по умолчанию, но есть несколько способов мониторинга системы.

---

## 🔧 Способ 1: Командная строка (htop/top)

### Установка htop (рекомендуется)
```bash
# Для Ubuntu/Debian
sudo apt update
sudo apt install htop -y

# Для CentOS/RHEL
sudo yum install htop -y
```

### Использование htop
```bash
htop
```

**Горячие клавиши:**
- `F2` - Настройки
- `F3` - Поиск процесса
- `F4` - Фильтр
- `F5` - Дерево процессов
- `F6` - Сортировка
- `F9` - Убить процесс
- `F10` - Выход
- `q` - Выход

### Использование top (встроенный)
```bash
top
```

**Горячие клавиши:**
- `q` - Выход
- `k` - Убить процесс (потребует PID)
- `M` - Сортировка по памяти
- `P` - Сортировка по CPU
- `1` - Показать все CPU ядра

---

## 🎨 Способ 2: Веб-интерфейс (рекомендуется для вас)

Я создал веб-интерфейс для мониторинга VPS, который доступен по адресу:

### Локальный доступ
```
http://localhost:3000/admin/monitor
```

### Удаленный доступ (если настроен)
```
https://your-domain.com/admin/monitor
```

### Что показывает интерфейс:
- 📊 **CPU Usage** - Использование процессора (%)
- 💾 **Memory Usage** - Использование памяти (MB/GB)
- 💿 **Disk Usage** - Использование диска (GB)
- 🌐 **Network** - Сетевая активность (входящий/исходящий трафик)
- 🔄 **Uptime** - Время работы сервера
- 📈 **Load Average** - Средняя нагрузка системы

---

## 🛠️ Способ 3: Установка веб-интерфейса (Netdata/Grafana)

### Netdata (простой и красивый)
```bash
# Установка Netdata
bash <(curl -Ss https://my-netdata.io/kickstart.sh)

# После установки доступен по адресу:
# http://your-vps-ip:19999
```

### Grafana + Prometheus (продвинутый)
```bash
# Установка через Docker Compose
cd ~/FreeDip/backend
# Добавьте в docker-compose.yml сервисы для мониторинга
```

---

## 📊 Способ 4: Полезные команды для мониторинга

### Просмотр процессов
```bash
# Все процессы
ps aux

# Процессы с фильтром
ps aux | grep nginx
ps aux | grep dotnet
ps aux | grep postgres
```

### Использование памяти
```bash
# Краткая информация
free -h

# Детальная информация
cat /proc/meminfo
```

### Использование диска
```bash
# Использование дисков
df -h

# Размер папок
du -sh /var/*
du -sh ~/FreeDip/*
```

### Использование CPU
```bash
# Информация о CPU
lscpu

# Нагрузка
uptime
```

### Сетевые соединения
```bash
# Активные соединения
netstat -tulpn

# Или
ss -tulpn
```

### Логи системы
```bash
# Системные логи
journalctl -f

# Логи Docker
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f api
docker-compose logs -f postgres
```

---

## 🎯 Где находится веб-интерфейс мониторинга

### В проекте FreeDip

Файл: `app/admin/monitor/page.tsx`

**Как открыть:**
1. Войдите в систему как администратор
2. Перейдите по адресу: `/admin/monitor`
3. Или через меню: **Админка** → **Мониторинг**

### API для получения статистики

Файл: `app/api/admin/system-stats/route.ts`

**Эндпоинт:** `GET /api/admin/system-stats`

**Ответ:**
```json
{
  "cpu": {
    "usage": 25.5,
    "cores": 4
  },
  "memory": {
    "used": 2048,
    "total": 4096,
    "percentage": 50
  },
  "disk": {
    "used": 50,
    "total": 100,
    "percentage": 50
  },
  "network": {
    "incoming": 1024,
    "outgoing": 512
  },
  "uptime": 86400
}
```

---

## 🔐 Настройка доступа к мониторингу

### Требования:
1. Пользователь должен быть авторизован
2. Пользователь должен иметь роль `admin`

### Проверка роли:
В `app/admin/monitor/page.tsx` есть проверка:
```typescript
const { user, userRole } = useAuth()

if (userRole !== 'admin') {
  return <div>Доступ запрещен</div>
}
```

---

## 📱 Мобильная версия

Веб-интерфейс мониторинга адаптивен и работает на мобильных устройствах.

---

## 🚀 Автоматическое обновление данных

Данные обновляются каждые 5 секунд автоматически через `setInterval`.

---

## 🛡️ Безопасность

⚠️ **ВАЖНО**: 
- Мониторинг доступен только администраторам
- Не публикуйте URL мониторинга публично
- Используйте HTTPS для production
- Ограничьте доступ по IP если возможно

---

## 📝 Полезные команды для быстрого мониторинга

```bash
# Быстрый обзор системы
htop

# Использование ресурсов Docker
docker stats

# Логи в реальном времени
docker-compose logs -f

# Проверка места на диске
df -h

# Проверка памяти
free -h

# Проверка нагрузки
uptime
```

---

## 🎨 Кастомизация интерфейса

Вы можете изменить интерфейс мониторинга, отредактировав:
- `app/admin/monitor/page.tsx` - основной компонент
- `app/api/admin/system-stats/route.ts` - API для получения данных

---

## 🔗 Дополнительные инструменты

### Glances (альтернатива htop)
```bash
pip install glances
glances
```

### Btop (современная альтернатива)
```bash
# Установка
sudo apt install btop -y
# Использование
btop
```

---

**Готово!** Теперь вы знаете, где находится диспетчер задач на VPS и как им пользоваться! 🎉




