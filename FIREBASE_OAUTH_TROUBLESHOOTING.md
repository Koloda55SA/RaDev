# 🔧 Решение проблемы Google OAuth - 404 на /__/firebase/init.json

## Проблема

При попытке входа через Google возникает ошибка 404 на `/__/firebase/init.json`. Это **НЕ критично** - Firebase Auth работает без этого файла, используя конфигурацию из кода.

## ✅ Решение

### 1. Проверьте Authorized Domains в Firebase

**КРИТИЧНО:** Домен должен быть добавлен в Firebase Console:

1. Откройте https://console.firebase.google.com/
2. Выберите проект `freedip-27d92`
3. Перейдите в **Authentication** → **Settings** → **Authorized domains**
4. **Добавьте ваш текущий домен Vercel:**
   - Найдите актуальный домен в Vercel Dashboard
   - Добавьте его в список Authorized domains
   - Например: `radev-test-2-xxxxx-kolodas-projects.vercel.app`

### 2. Проверьте OAuth Redirect URIs в Google Cloud Console

1. Откройте https://console.cloud.google.com/
2. Выберите проект `freedip-27d92`
3. Перейдите в **APIs & Services** → **Credentials**
4. Найдите **OAuth 2.0 Client ID** (Web client для Firebase)
5. В **"Authorized redirect URIs"** добавьте:
   - `https://[ваш-текущий-домен-vercel]/__/auth/handler`
   - Например: `https://radev-test-2-xxxxx-kolodas-projects.vercel.app/__/auth/handler`

### 3. О 404 на /__/firebase/init.json

Этот файл нужен только если проект развернут на Firebase Hosting. Поскольку проект на Vercel, эта ошибка **не влияет** на работу OAuth. Firebase Auth использует конфигурацию из `lib/firebase/config.ts`.

### 4. Как найти актуальный домен

1. Откройте Vercel Dashboard: https://vercel.com/dashboard
2. Найдите проект `radev-test-2`
3. Скопируйте Production URL
4. Добавьте его в Firebase Authorized domains

## 🚨 Важно

После добавления домена в Firebase подождите **5-10 минут** для применения изменений!

## 📝 Проверка

После настройки:
1. Очистите кеш браузера
2. Откройте страницу логина
3. Нажмите "Войти через Google"
4. Проверьте консоль - не должно быть ошибок авторизации






