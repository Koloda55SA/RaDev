// Реальная генерация контента для адаптивного обучения
// Заполняет заглушки в adaptiveLearning.ts

import { Language } from './courseData'
// import type { AdaptiveContent } from './adaptiveLearning'
export interface AdaptiveContent {
  // Placeholder interface
}

export class ContentGenerator {
  // Генерация простого объяснения
  generateSimpleExplanation(
    language: Language,
    topic: string,
    difficulty: number
  ): string {
    const langNames: Partial<Record<Language, string>> = {
      python: 'Python',
      javascript: 'JavaScript',
      java: 'Java',
      cpp: 'C++',
      csharp: 'C#',
      html: 'HTML',
      css: 'CSS'
    }
    
    const langName = langNames[language] || language
    const topicLower = topic.toLowerCase()
    
    // Базовые темы
    if (topicLower.includes('введение') || topicLower.includes('intro')) {
      return `**${topic}** - это начало вашего пути в изучении ${langName}. Здесь вы узнаете основы языка, его историю и где он применяется. ${langName} - это мощный инструмент для создания программ любого уровня сложности.`
    }
    
    if (topicLower.includes('переменн') || topicLower.includes('variable')) {
      return `**Переменные** в ${langName} - это именованные контейнеры для хранения данных. Представьте переменную как коробку с названием: вы можете положить в неё значение и использовать это название для доступа к значению. Например: \`x = 5\` создает переменную x со значением 5.`
    }
    
    if (topicLower.includes('оператор') || topicLower.includes('operator')) {
      return `**Операторы** в ${langName} - это символы, которые выполняют операции над данными. Есть арифметические операторы (+, -, *, /), операторы сравнения (==, !=, <, >) и логические операторы (and, or, not). Они позволяют выполнять вычисления и принимать решения в программе.`
    }
    
    if (topicLower.includes('услов') || topicLower.includes('if') || topicLower.includes('condition')) {
      return `**Условные операторы** позволяют программе принимать решения. Если условие истинно - выполняется один блок кода, если ложно - другой. Это как развилка на дороге: в зависимости от ситуации выбирается нужный путь.`
    }
    
    if (topicLower.includes('цикл') || topicLower.includes('loop') || topicLower.includes('for') || topicLower.includes('while')) {
      return `**Циклы** позволяют повторять код многократно. Вместо того чтобы писать один и тот же код 100 раз, вы пишете его один раз в цикле, и он выполнится нужное количество раз. Это как конвейер на заводе - одна и та же операция повторяется для каждого элемента.`
    }
    
    if (topicLower.includes('функц') || topicLower.includes('function') || topicLower.includes('метод')) {
      return `**Функции** - это блоки кода, которые можно вызывать многократно. Функция принимает входные данные (параметры), выполняет действия и может возвращать результат. Это как рецепт: вы даете ингредиенты, функция готовит и возвращает блюдо.`
    }
    
    if (topicLower.includes('массив') || topicLower.includes('список') || topicLower.includes('list') || topicLower.includes('array')) {
      return `**Массивы/Списки** - это коллекции элементов, упорядоченные в определенном порядке. Представьте список покупок: каждый элемент имеет свой номер (индекс), и вы можете добавлять, удалять и изменять элементы.`
    }
    
    if (topicLower.includes('класс') || topicLower.includes('class') || topicLower.includes('объект')) {
      return `**Классы** - это шаблоны для создания объектов. Класс описывает структуру и поведение, а объект - это конкретный экземпляр класса. Это как чертеж дома (класс) и построенный по нему дом (объект).`
    }
    
    if (topicLower.includes('база данных') || topicLower.includes('database') || topicLower.includes('sql')) {
      return `**Базы данных** - это организованное хранилище данных. Представьте библиотеку: книги (данные) хранятся в определенном порядке, и вы можете быстро найти нужную книгу. Базы данных позволяют эффективно хранить, искать и изменять большие объемы информации.`
    }
    
    if (topicLower.includes('api') || topicLower.includes('rest')) {
      return `**API (Application Programming Interface)** - это способ взаимодействия между программами. API определяет правила, по которым одна программа может запросить данные или услуги у другой. Это как меню в ресторане: вы видите доступные блюда (функции) и можете заказать их.`
    }
    
    if (topicLower.includes('тест') || topicLower.includes('test')) {
      return `**Тестирование** - это проверка правильности работы кода. Тесты автоматически проверяют, что ваш код работает как ожидается. Это как контрольная работа для программы: если все тесты пройдены - код работает правильно.`
    }
    
    if (topicLower.includes('docker')) {
      return `**Docker** - это платформа для контейнеризации приложений. Контейнер упаковывает приложение со всеми его зависимостями в один пакет, который работает одинаково на любой системе. Это как упаковка товара: независимо от того, куда его отправят, содержимое остается неизменным.`
    }
    
    // Общий случай
    return `**${topic}** - это важная концепция в ${langName}, которая позволяет ${this.getTopicPurpose(topicLower)}. В этом уроке мы изучим основы и научимся применять это на практике.`
  }
  
  // Генерация детального объяснения
  generateDetailedExplanation(
    language: Language,
    topic: string,
    difficulty: number
  ): string {
    const simple = this.generateSimpleExplanation(language, topic, difficulty)
    
    return `${simple}\n\n## Как это работает?\n\n${this.generateHowItWorks(language, topic, difficulty)}\n\n## Примеры использования\n\n${this.generateUsageExamples(language, topic, difficulty)}\n\n## Частые ошибки\n\n${this.generateCommonMistakes(language, topic)}`
  }
  
  // Генерация аналогий
  getAnalogies(topic: string, style: 'visual' | 'kinesthetic' | 'auditory' | 'reading'): string[] {
    const topicLower = topic.toLowerCase()
    const analogies: string[] = []
    
    if (topicLower.includes('переменн')) {
      analogies.push('Переменная - это как ячейка в почтовом ящике: у неё есть адрес (имя) и содержимое (значение)')
      analogies.push('Представьте переменную как стикер на коробке: название на стикере - это имя переменной, а содержимое коробки - значение')
    }
    
    if (topicLower.includes('функц')) {
      analogies.push('Функция - это как кофемашина: вы нажимаете кнопку (вызываете функцию), даете ингредиенты (параметры), и получаете кофе (результат)')
      analogies.push('Функция похожа на рецепт: у вас есть список ингредиентов (параметры), вы следуете инструкциям (код функции) и получаете блюдо (результат)')
    }
    
    if (topicLower.includes('цикл')) {
      analogies.push('Цикл - это как конвейер: один и тот же процесс повторяется для каждого элемента')
      analogies.push('Представьте цикл как повторение упражнения: вы делаете одно и то же движение несколько раз, пока не достигнете цели')
    }
    
    if (topicLower.includes('класс')) {
      analogies.push('Класс - это как чертеж дома: он описывает, как должен выглядеть дом, но сам по себе не является домом. Объект - это конкретный дом, построенный по чертежу')
      analogies.push('Класс похож на форму для печенья: форма определяет форму печенья, а каждое печенье (объект) - это конкретный экземпляр')
    }
    
    if (topicLower.includes('база данных')) {
      analogies.push('База данных - это как библиотека: книги (данные) организованы по полкам (таблицы), и библиотекарь (СУБД) помогает найти нужную книгу')
      analogies.push('Представьте базу данных как склад: товары (данные) хранятся в определенных местах, и система знает, где что находится')
    }
    
    if (analogies.length === 0) {
      analogies.push(`Изучение ${topic} похоже на изучение нового инструмента: сначала вы узнаете основы, затем практикуетесь, и со временем становитесь мастером`)
    }
    
    return analogies
  }
  
  // Генерация визуальных метафор
  getVisualMetaphors(topic: string): string[] {
    const topicLower = topic.toLowerCase()
    const metaphors: string[] = []
    
    if (topicLower.includes('цикл')) {
      metaphors.push('🔄 Цикл можно представить как спираль: каждый виток - одна итерация, движение идет по кругу до достижения условия остановки')
      metaphors.push('📊 Визуализация цикла: поток данных движется по кругу, обрабатывая каждый элемент')
    }
    
    if (topicLower.includes('функц')) {
      metaphors.push('⚙️ Функция - это машина: входные данные поступают, обрабатываются, и выходит результат')
      metaphors.push('🔧 Представьте функцию как инструмент в ящике: вы берете его, используете для задачи, и возвращаете обратно')
    }
    
    if (topicLower.includes('класс')) {
      metaphors.push('🏗️ Класс - это архитектурный план: он описывает структуру, но не является зданием. Объекты - это построенные здания')
      metaphors.push('🌳 Класс можно представить как дерево: корни (базовый класс), ствол (текущий класс), ветви (наследование)')
    }
    
    return metaphors
  }
  
  // Генерация минимального примера
  generateMinimalExample(language: Language, topic: string): string {
    const topicLower = topic.toLowerCase()
    
    if (language === 'python') {
      if (topicLower.includes('переменн')) {
        return `# Простейший пример переменных
name = "Иван"
age = 25
print(f"Имя: {name}, Возраст: {age}")`
      }
      
      if (topicLower.includes('цикл')) {
        return `# Простой цикл
for i in range(5):
    print(f"Число: {i}")`
      }
      
      if (topicLower.includes('функц')) {
        return `# Простая функция
def greet(name):
    return f"Привет, {name}!"

print(greet("Мир"))`
      }
      
      if (topicLower.includes('класс')) {
        return `# Простой класс
class Person:
    def __init__(self, name):
        self.name = name
    
    def greet(self):
        return f"Привет, я {self.name}"

person = Person("Анна")
print(person.greet())`
      }
    }
    
    return `# Минимальный пример для ${topic}\n# TODO: реализовать`
  }
  
  // Генерация production примера
  generateProductionExample(
    language: Language,
    topic: string,
    path: 'fast' | 'fundamental' | 'project-based' | 'interview-prep'
  ): string {
    const minimal = this.generateMinimalExample(language, topic)
    
    if (path === 'project-based') {
      return `${minimal}\n\n# Production версия с обработкой ошибок\n# TODO: добавить error handling, logging, валидацию`
    }
    
    if (path === 'interview-prep') {
      return `${minimal}\n\n# Версия для собеседования с edge cases\n# TODO: добавить обработку граничных случаев`
    }
    
    return minimal
  }
  
  // Генерация оптимизированного примера
  generateOptimizedExample(language: Language, topic: string, difficulty: number): string {
    const minimal = this.generateMinimalExample(language, topic)
    
    if (difficulty >= 7) {
      return `${minimal}\n\n# Оптимизированная версия\n# TODO: добавить оптимизации производительности`
    }
    
    return minimal
  }
  
  // Генерация примера с тестами
  generateExampleWithTests(language: Language, topic: string): string {
    const minimal = this.generateMinimalExample(language, topic)
    
    if (language === 'python') {
      return `${minimal}\n\n# Тесты\nimport unittest\n\nclass TestExample(unittest.TestCase):\n    def test_basic(self):\n        # TODO: написать тесты\n        pass\n\nif __name__ == '__main__':\n    unittest.main()`
    }
    
    return `${minimal}\n\n# Тесты\n# TODO: добавить unit тесты`
  }
  
  // Генерация упражнения с подсказками
  generateExerciseWithHints(topic: string, difficulty: number): string {
    const topicLower = topic.toLowerCase()
    
    if (topicLower.includes('переменн')) {
      return `**Задание:** Создайте три переменные: имя (строка), возраст (число) и город (строка). Выведите информацию о человеке.\n\n**Подсказки:**\n1. Используйте знак = для присваивания\n2. Строки заключаются в кавычки\n3. Используйте print() для вывода`
    }
    
    if (topicLower.includes('цикл')) {
      return `**Задание:** Выведите все четные числа от 0 до 20.\n\n**Подсказки:**\n1. Используйте цикл for с range()\n2. Проверяйте остаток от деления на 2\n3. Если остаток 0 - число четное`
    }
    
    if (topicLower.includes('функц')) {
      return `**Задание:** Создайте функцию, которая принимает два числа и возвращает их сумму.\n\n**Подсказки:**\n1. Используйте def для создания функции\n2. Параметры указываются в скобках\n3. Используйте return для возврата значения`
    }
    
    return `**Задание:** Реализуйте ${topic}\n\n**Подсказки:**\n1. Вспомните теорию урока\n2. Начните с простого\n3. Тестируйте по ходу`
  }
  
  // Генерация упражнения с несколькими решениями
  generateMultipleSolutions(topic: string): string {
    return `**Задание:** ${this.generateExerciseWithHints(topic, 5)}\n\n**Варианты решения:**\n1. Базовое решение (простое, но не оптимальное)\n2. Оптимизированное решение (быстрее, но сложнее)\n3. Элегантное решение (краткое и читаемое)\n\nПопробуйте реализовать все три варианта!`
  }
  
  // Генерация упражнения с ограничениями
  generateWithConstraints(topic: string, difficulty: number): string {
    const constraints = difficulty >= 7 
      ? 'Ограничение: O(n) по времени, O(1) по памяти'
      : difficulty >= 5
      ? 'Ограничение: не более 10 строк кода'
      : 'Ограничение: используйте только изученные конструкции'
    
    return `${this.generateExerciseWithHints(topic, difficulty)}\n\n**${constraints}**`
  }
  
  // Генерация реального упражнения
  generateRealWorldExercise(topic: string, goals: string[]): string {
    const topicLower = topic.toLowerCase()
    let context = ''
    
    if (goals.includes('web-dev')) {
      context = 'Веб-приложение'
    } else if (goals.includes('data-science')) {
      context = 'Анализ данных'
    } else if (goals.includes('job-interview')) {
      context = 'Техническое собеседование'
    } else {
      context = 'Реальный проект'
    }
    
    if (topicLower.includes('api')) {
      return `**Реальное задание:** Создайте API endpoint для ${context}, который принимает запрос и возвращает данные в формате JSON.\n\n**Контекст:** В реальных проектах API используются для взаимодействия между фронтендом и бэкендом.`
    }
    
    if (topicLower.includes('база данных')) {
      return `**Реальное задание:** Спроектируйте схему базы данных для ${context} с таблицами пользователей и их заказов.\n\n**Контекст:** В реальных приложениях базы данных хранят всю важную информацию.`
    }
    
    return `**Реальное задание:** Примените ${topic} в контексте ${context}.\n\n**Контекст:** Это задание моделирует реальную ситуацию из практики разработки.`
  }
  
  // Вспомогательные методы
  private getTopicPurpose(topicLower: string): string {
    if (topicLower.includes('переменн')) return 'хранить данные'
    if (topicLower.includes('цикл')) return 'повторять действия'
    if (topicLower.includes('функц')) return 'организовать код'
    if (topicLower.includes('класс')) return 'структурировать данные'
    return 'решать задачи'
  }
  
  private generateHowItWorks(language: Language, topic: string, difficulty: number): string {
    return `Механизм работы ${topic} основан на ${this.getMechanism(topic)}. При выполнении кода происходит следующее: ${this.getProcess(topic)}.`
  }
  
  private generateUsageExamples(language: Language, topic: string, difficulty: number): string {
    return `Примеры использования ${topic}:\n${this.generateMinimalExample(language, topic)}`
  }
  
  private generateCommonMistakes(language: Language, topic: string): string {
    const topicLower = topic.toLowerCase()
    
    if (topicLower.includes('переменн')) {
      return '- Использование неинициализированных переменных\n- Путаница между изменяемыми и неизменяемыми типами\n- Неправильные имена переменных'
    }
    
    if (topicLower.includes('цикл')) {
      return '- Бесконечные циклы (отсутствие условия выхода)\n- Изменение списка во время итерации\n- Неправильное использование break/continue'
    }
    
    if (topicLower.includes('функц')) {
      return '- Изменяемые аргументы по умолчанию\n- Непонимание области видимости\n- Забывание return'
    }
    
    return '- Недостаточное понимание концепции\n- Неправильное применение\n- Игнорирование best practices'
  }
  
  private getMechanism(topic: string): string {
    const topicLower = topic.toLowerCase()
    if (topicLower.includes('цикл')) return 'итерации по элементам'
    if (topicLower.includes('функц')) return 'вызове блока кода'
    if (topicLower.includes('класс')) return 'создании объектов'
    return 'выполнении операций'
  }
  
  private getProcess(topic: string): string {
    const topicLower = topic.toLowerCase()
    if (topicLower.includes('цикл')) return 'код выполняется для каждого элемента, пока не будет достигнуто условие остановки'
    if (topicLower.includes('функц')) return 'параметры передаются в функцию, выполняется код, возвращается результат'
    if (topicLower.includes('класс')) return 'создается объект на основе класса, инициализируются атрибуты, доступны методы'
    return 'выполняются операции согласно логике'
  }
}

