# Руководство по добавлению контента

## 📝 Добавление проекта в портфолио

### Через админку

1. Войдите в админку (`/admin/login`)
2. Заполните форму:
   - Название проекта
   - Описание
   - Автор (Сыймыкбек / Абдыкадыр / Оба)
   - Тип (Веб / Мобильное / Десктоп / Диплом)
   - Технологии (через запятую)
   - Год
3. Нажмите "Добавить"

### Через код

Отредактируйте `app/portfolio/page.tsx` и добавьте проект в массив `mockProjects`:

```typescript
{
  id: 'unique-id',
  title: 'Название проекта',
  description: 'Описание проекта',
  author: 'syyimyk' | 'abdykadyr' | 'both',
  type: 'web' | 'mobile' | 'desktop' | 'diploma',
  technologies: ['React', 'Next.js'],
  year: 2024,
  github: 'https://github.com/...',
  demo: 'https://demo.com',
  download: 'https://download.com',
}
```

## 📰 Добавление статьи в блог

Отредактируйте `app/blog/page.tsx` и добавьте в массив `mockPosts`:

```typescript
{
  id: 'unique-id',
  title: 'Заголовок статьи',
  excerpt: 'Краткое описание',
  author: 'syyimyk' | 'abdykadyr',
  date: '2024-01-15',
  category: 'Web Development',
  readTime: '10 мин',
}
```

Создайте страницу статьи в `app/blog/[id]/page.tsx`

## 💬 Добавление отзыва

Отредактируйте `app/reviews/page.tsx` и добавьте в массив `mockReviews`:

```typescript
{
  id: 'unique-id',
  author: 'Имя клиента',
  project: 'Тип проекта',
  rating: 5,
  text: 'Текст отзыва',
  date: '2024-01-10',
}
```

## 🌍 Добавление перевода

1. Откройте `public/locales/ru.json` или `public/locales/ky.json`
2. Добавьте новый ключ и значение
3. Используйте в компоненте:

```tsx
const { t } = useTranslation()
t('your.key')
```

## 🎨 Изменение стилей

- Глобальные стили: `app/globals.css`
- Цветовая схема: `tailwind.config.ts`
- Компоненты: `components/ui/`

## 🔧 Добавление новой страницы

1. Создайте папку в `app/` (например, `app/new-page/`)
2. Создайте `page.tsx`:

```tsx
export default function NewPage() {
  return <div>Содержимое страницы</div>
}
```

3. Добавьте ссылку в `components/layout/Header.tsx`






## 📝 Добавление проекта в портфолио

### Через админку

1. Войдите в админку (`/admin/login`)
2. Заполните форму:
   - Название проекта
   - Описание
   - Автор (Сыймыкбек / Абдыкадыр / Оба)
   - Тип (Веб / Мобильное / Десктоп / Диплом)
   - Технологии (через запятую)
   - Год
3. Нажмите "Добавить"

### Через код

Отредактируйте `app/portfolio/page.tsx` и добавьте проект в массив `mockProjects`:

```typescript
{
  id: 'unique-id',
  title: 'Название проекта',
  description: 'Описание проекта',
  author: 'syyimyk' | 'abdykadyr' | 'both',
  type: 'web' | 'mobile' | 'desktop' | 'diploma',
  technologies: ['React', 'Next.js'],
  year: 2024,
  github: 'https://github.com/...',
  demo: 'https://demo.com',
  download: 'https://download.com',
}
```

## 📰 Добавление статьи в блог

Отредактируйте `app/blog/page.tsx` и добавьте в массив `mockPosts`:

```typescript
{
  id: 'unique-id',
  title: 'Заголовок статьи',
  excerpt: 'Краткое описание',
  author: 'syyimyk' | 'abdykadyr',
  date: '2024-01-15',
  category: 'Web Development',
  readTime: '10 мин',
}
```

Создайте страницу статьи в `app/blog/[id]/page.tsx`

## 💬 Добавление отзыва

Отредактируйте `app/reviews/page.tsx` и добавьте в массив `mockReviews`:

```typescript
{
  id: 'unique-id',
  author: 'Имя клиента',
  project: 'Тип проекта',
  rating: 5,
  text: 'Текст отзыва',
  date: '2024-01-10',
}
```

## 🌍 Добавление перевода

1. Откройте `public/locales/ru.json` или `public/locales/ky.json`
2. Добавьте новый ключ и значение
3. Используйте в компоненте:

```tsx
const { t } = useTranslation()
t('your.key')
```

## 🎨 Изменение стилей

- Глобальные стили: `app/globals.css`
- Цветовая схема: `tailwind.config.ts`
- Компоненты: `components/ui/`

## 🔧 Добавление новой страницы

1. Создайте папку в `app/` (например, `app/new-page/`)
2. Создайте `page.tsx`:

```tsx
export default function NewPage() {
  return <div>Содержимое страницы</div>
}
```

3. Добавьте ссылку в `components/layout/Header.tsx`











