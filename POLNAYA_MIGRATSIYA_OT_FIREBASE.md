# 🔄 Полная миграция от Firebase к C# бэкенду

## ✅ Что уже сделано

### 1. Создана инфраструктура для C# API
- ✅ `lib/api/config.ts` - Конфигурация API
- ✅ `lib/api/client.ts` - API клиент для взаимодействия с бэкендом
- ✅ `lib/api/authAdapter.ts` - Адаптер для аутентификации

### 2. Созданы провайдеры аутентификации
- ✅ `components/auth/ApiAuthProvider.tsx` - Провайдер для C# API
- ✅ `components/auth/HybridAuthProvider.tsx` - Гибридный провайдер (автоматическое переключение)
- ✅ `components/auth/useAuth.ts` - Единый хук useAuth

### 3. Настроен Google OAuth
- ✅ `app/auth/google/callback/page.tsx` - Страница обработки callback
- ✅ `app/api/auth/google-callback/route.ts` - API route для обмена кода на токен

### 4. Обновлен layout
- ✅ `app/layout.tsx` - Использует HybridAuthProvider вместо AuthProvider

---

## 📋 Что нужно сделать дальше

### 1. Замена Firebase Firestore на C# API

#### Файлы, которые используют Firestore:
- `app/chat/page.tsx` - Чат (сообщения)
- `app/profile/page.tsx` - Профиль пользователя
- `app/admin/page.tsx` - Админка
- `components/ai/ChatBot.tsx` - ИИ чат-бот
- И другие...

#### План замены:
1. Заменить `collection()`, `doc()`, `addDoc()`, `updateDoc()` на вызовы `apiClient`
2. Заменить `onSnapshot()` на polling или WebSocket (если нужно real-time)
3. Обновить типы данных для соответствия C# моделям

### 2. Замена Firebase Storage на C# API

#### Файлы, которые используют Storage:
- `app/profile/page.tsx` - Загрузка аватара
- `app/chat/page.tsx` - Загрузка файлов в чат
- `app/admin/page.tsx` - Загрузка файлов

#### План замены:
1. Заменить `ref()`, `uploadBytes()`, `getDownloadURL()` на `apiClient.uploadAvatar()` или `apiClient.sendMessage(file)`
2. Обновить URL для отображения файлов (использовать URL от C# API)

### 3. Обновление чата

#### Текущее состояние:
- Использует Firebase Firestore для хранения сообщений
- Использует `onSnapshot()` для real-time обновлений

#### Нужно:
1. Заменить на `apiClient.getGlobalChat()` и `apiClient.getPrivateChat()`
2. Реализовать polling или WebSocket для real-time обновлений
3. Использовать `apiClient.sendGlobalMessage()` и `apiClient.sendPrivateMessage()`

### 4. Обновление профиля

#### Текущее состояние:
- Использует Firebase Firestore для данных пользователя
- Использует Firebase Storage для аватара

#### Нужно:
1. Заменить на `apiClient.updateProfile()`
2. Заменить загрузку аватара на `apiClient.uploadAvatar()`
3. Обновить получение данных профиля через `apiClient.getCurrentUser()`

---

## 🔧 Пошаговая инструкция

### Шаг 1: Настройка переменных окружения

#### На Vercel:
```
NEXT_PUBLIC_API_URL=https://your-vps-domain.com/api
NEXT_PUBLIC_USE_API_BACKEND=true
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id
```

#### На VPS (в .env):
```
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
JWT_SECRET=your_jwt_secret
# ... другие переменные
```

### Шаг 2: Проверка работы аутентификации

1. Откройте `/login`
2. Попробуйте войти через email/password
3. Попробуйте войти через Google
4. Проверьте, что токен сохраняется в localStorage

### Шаг 3: Постепенная замена функций

Начните с простых функций:
1. ✅ Аутентификация (уже сделано)
2. ⏳ Профиль пользователя
3. ⏳ Загрузка аватара
4. ⏳ Чат
5. ⏳ Курсы и прогресс

---

## 🧪 Тестирование

### Локальное тестирование:
```bash
# Запуск бэкенда
cd backend
docker-compose up -d

# Запуск frontend
npm run dev

# Откройте http://localhost:3000
```

### Production тестирование:
1. Закоммитьте изменения
2. Задеплойте на Vercel
3. Проверьте работу на production домене

---

## ⚠️ Важные замечания

1. **Обратная совместимость**: HybridAuthProvider автоматически переключается на Firebase, если C# API недоступен
2. **Токены**: Токены хранятся в localStorage (можно улучшить безопасность позже)
3. **Real-time**: Для real-time обновлений нужно будет реализовать WebSocket или polling
4. **Миграция данных**: Нужно будет мигрировать существующие данные из Firebase в PostgreSQL

---

## 📝 Чек-лист миграции

- [x] Создана инфраструктура API
- [x] Настроена аутентификация
- [x] Настроен Google OAuth
- [ ] Заменен чат на C# API
- [ ] Заменен профиль на C# API
- [ ] Заменена загрузка файлов на C# API
- [ ] Заменены курсы на C# API
- [ ] Мигрированы данные из Firebase
- [ ] Протестировано на production

---

## 🔗 Полезные ссылки

- [Инструкция по Google OAuth](./GOOGLE_OAUTH_POLNAYA_INSTRUKTSIYA.md)
- [Инструкция по диспетчеру задач VPS](./VPS_DISPETCHER_ZADACH_INSTRUKTSIYA.md)
- [API Client документация](./lib/api/client.ts)

---

**Статус**: Базовая инфраструктура готова, аутентификация работает. Осталось заменить использование Firebase в компонентах на C# API.


## ✅ Что уже сделано

### 1. Создана инфраструктура для C# API
- ✅ `lib/api/config.ts` - Конфигурация API
- ✅ `lib/api/client.ts` - API клиент для взаимодействия с бэкендом
- ✅ `lib/api/authAdapter.ts` - Адаптер для аутентификации

### 2. Созданы провайдеры аутентификации
- ✅ `components/auth/ApiAuthProvider.tsx` - Провайдер для C# API
- ✅ `components/auth/HybridAuthProvider.tsx` - Гибридный провайдер (автоматическое переключение)
- ✅ `components/auth/useAuth.ts` - Единый хук useAuth

### 3. Настроен Google OAuth
- ✅ `app/auth/google/callback/page.tsx` - Страница обработки callback
- ✅ `app/api/auth/google-callback/route.ts` - API route для обмена кода на токен

### 4. Обновлен layout
- ✅ `app/layout.tsx` - Использует HybridAuthProvider вместо AuthProvider

---

## 📋 Что нужно сделать дальше

### 1. Замена Firebase Firestore на C# API

#### Файлы, которые используют Firestore:
- `app/chat/page.tsx` - Чат (сообщения)
- `app/profile/page.tsx` - Профиль пользователя
- `app/admin/page.tsx` - Админка
- `components/ai/ChatBot.tsx` - ИИ чат-бот
- И другие...

#### План замены:
1. Заменить `collection()`, `doc()`, `addDoc()`, `updateDoc()` на вызовы `apiClient`
2. Заменить `onSnapshot()` на polling или WebSocket (если нужно real-time)
3. Обновить типы данных для соответствия C# моделям

### 2. Замена Firebase Storage на C# API

#### Файлы, которые используют Storage:
- `app/profile/page.tsx` - Загрузка аватара
- `app/chat/page.tsx` - Загрузка файлов в чат
- `app/admin/page.tsx` - Загрузка файлов

#### План замены:
1. Заменить `ref()`, `uploadBytes()`, `getDownloadURL()` на `apiClient.uploadAvatar()` или `apiClient.sendMessage(file)`
2. Обновить URL для отображения файлов (использовать URL от C# API)

### 3. Обновление чата

#### Текущее состояние:
- Использует Firebase Firestore для хранения сообщений
- Использует `onSnapshot()` для real-time обновлений

#### Нужно:
1. Заменить на `apiClient.getGlobalChat()` и `apiClient.getPrivateChat()`
2. Реализовать polling или WebSocket для real-time обновлений
3. Использовать `apiClient.sendGlobalMessage()` и `apiClient.sendPrivateMessage()`

### 4. Обновление профиля

#### Текущее состояние:
- Использует Firebase Firestore для данных пользователя
- Использует Firebase Storage для аватара

#### Нужно:
1. Заменить на `apiClient.updateProfile()`
2. Заменить загрузку аватара на `apiClient.uploadAvatar()`
3. Обновить получение данных профиля через `apiClient.getCurrentUser()`

---

## 🔧 Пошаговая инструкция

### Шаг 1: Настройка переменных окружения

#### На Vercel:
```
NEXT_PUBLIC_API_URL=https://your-vps-domain.com/api
NEXT_PUBLIC_USE_API_BACKEND=true
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id
```

#### На VPS (в .env):
```
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
JWT_SECRET=your_jwt_secret
# ... другие переменные
```

### Шаг 2: Проверка работы аутентификации

1. Откройте `/login`
2. Попробуйте войти через email/password
3. Попробуйте войти через Google
4. Проверьте, что токен сохраняется в localStorage

### Шаг 3: Постепенная замена функций

Начните с простых функций:
1. ✅ Аутентификация (уже сделано)
2. ⏳ Профиль пользователя
3. ⏳ Загрузка аватара
4. ⏳ Чат
5. ⏳ Курсы и прогресс

---

## 🧪 Тестирование

### Локальное тестирование:
```bash
# Запуск бэкенда
cd backend
docker-compose up -d

# Запуск frontend
npm run dev

# Откройте http://localhost:3000
```

### Production тестирование:
1. Закоммитьте изменения
2. Задеплойте на Vercel
3. Проверьте работу на production домене

---

## ⚠️ Важные замечания

1. **Обратная совместимость**: HybridAuthProvider автоматически переключается на Firebase, если C# API недоступен
2. **Токены**: Токены хранятся в localStorage (можно улучшить безопасность позже)
3. **Real-time**: Для real-time обновлений нужно будет реализовать WebSocket или polling
4. **Миграция данных**: Нужно будет мигрировать существующие данные из Firebase в PostgreSQL

---

## 📝 Чек-лист миграции

- [x] Создана инфраструктура API
- [x] Настроена аутентификация
- [x] Настроен Google OAuth
- [ ] Заменен чат на C# API
- [ ] Заменен профиль на C# API
- [ ] Заменена загрузка файлов на C# API
- [ ] Заменены курсы на C# API
- [ ] Мигрированы данные из Firebase
- [ ] Протестировано на production

---

## 🔗 Полезные ссылки

- [Инструкция по Google OAuth](./GOOGLE_OAUTH_POLNAYA_INSTRUKTSIYA.md)
- [Инструкция по диспетчеру задач VPS](./VPS_DISPETCHER_ZADACH_INSTRUKTSIYA.md)
- [API Client документация](./lib/api/client.ts)

---

**Статус**: Базовая инфраструктура готова, аутентификация работает. Осталось заменить использование Firebase в компонентах на C# API.




