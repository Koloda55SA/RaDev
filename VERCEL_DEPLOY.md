# 🚀 Деплой на Vercel

## Способ 1: Через Vercel Dashboard (Рекомендуется)

### Шаг 1: Подключение репозитория
1. Зайдите на [vercel.com](https://vercel.com) и войдите через GitHub
2. Нажмите **"Add New Project"**
3. Выберите репозиторий `Koloda55SA/RaDev`
4. Нажмите **"Import"**

### Шаг 2: Настройка переменных окружения

В разделе **"Environment Variables"** добавьте следующие переменные:

#### Обязательные переменные:
```
NEXT_PUBLIC_API_URL=http://213.199.56.27:5000/api
NEXT_PUBLIC_SITE_URL=https://your-domain.vercel.app
```

#### Опциональные переменные (для полной функциональности):
```
# OpenAI API для AI чат-бота
OPENAI_API_KEY=sk-your-openai-api-key

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Google Analytics (опционально)
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Google Search Console Verification (опционально)
NEXT_PUBLIC_GOOGLE_VERIFICATION=your-verification-code
```

### Шаг 3: Настройка проекта
- **Framework Preset**: Next.js (автоматически определится)
- **Root Directory**: `./` (корень проекта)
- **Build Command**: `npm run build` (по умолчанию)
- **Output Directory**: `.next` (по умолчанию)
- **Install Command**: `npm install` (по умолчанию)

### Шаг 4: Деплой
1. Нажмите **"Deploy"**
2. Дождитесь завершения сборки (обычно 2-5 минут)
3. После успешного деплоя получите URL вида: `https://your-project.vercel.app`

## Способ 2: Через Vercel CLI

### Шаг 1: Установка Vercel CLI
```bash
npm i -g vercel
```

### Шаг 2: Логин
```bash
vercel login
```

### Шаг 3: Деплой
```bash
# Первый деплой (preview)
vercel

# Production деплой
vercel --prod
```

### Шаг 4: Настройка переменных окружения через CLI
```bash
# Добавить переменную
vercel env add NEXT_PUBLIC_API_URL
# Введите значение: http://213.199.56.27:5000/api
# Выберите окружения: Production, Preview, Development

# Добавить остальные переменные аналогично
vercel env add OPENAI_API_KEY
vercel env add GOOGLE_CLIENT_ID
vercel env add GOOGLE_CLIENT_SECRET
vercel env add NEXT_PUBLIC_SITE_URL
```

## 🔧 Настройка домена

### Шаг 1: Добавление домена
1. В Vercel Dashboard перейдите в **Settings** → **Domains**
2. Добавьте ваш домен (например, `radev.digital`)
3. Следуйте инструкциям для настройки DNS записей

### Шаг 2: DNS настройки
Добавьте в настройки DNS вашего домена:
- **Type**: CNAME
- **Name**: @ (или www)
- **Value**: `cname.vercel-dns.com`

### Шаг 3: SSL сертификат
Vercel автоматически выдаст SSL сертификат через Let's Encrypt (обычно в течение нескольких минут)

## 📋 Чеклист перед деплоем

- [ ] Все переменные окружения добавлены в Vercel
- [ ] `NEXT_PUBLIC_API_URL` указывает на правильный адрес бэкенда
- [ ] `NEXT_PUBLIC_SITE_URL` установлен на финальный URL сайта
- [ ] Firebase конфигурация корректна (уже в коде)
- [ ] Google OAuth настроен (если используется)
- [ ] OpenAI API ключ добавлен (если используется AI чат)
- [ ] Код закоммичен и запушен в GitHub

## 🐛 Решение проблем

### Ошибка: "Build failed"
- Проверьте логи сборки в Vercel Dashboard
- Убедитесь, что все зависимости установлены
- Проверьте синтаксис TypeScript/JavaScript

### Ошибка: "Environment variable not found"
- Убедитесь, что переменные добавлены для всех окружений (Production, Preview, Development)
- Пересоберите проект после добавления переменных

### Ошибка: "API connection failed"
- Проверьте, что `NEXT_PUBLIC_API_URL` правильный
- Убедитесь, что бэкенд доступен с Vercel серверов
- Проверьте CORS настройки на бэкенде

### Медленная загрузка
- Убедитесь, что включены оптимизации в `next.config.js`
- Проверьте размер bundle через Vercel Analytics
- Используйте lazy loading для тяжелых компонентов

## 📊 Мониторинг

После деплоя вы можете:
- Просматривать аналитику в Vercel Dashboard
- Настроить Vercel Analytics для отслеживания производительности
- Использовать Vercel Speed Insights для мониторинга скорости загрузки

## 🔄 Автоматический деплой

Vercel автоматически деплоит проект при каждом push в:
- `main` branch → Production
- Другие branches → Preview deployments

Вы можете настроить это в **Settings** → **Git**

## 📝 Полезные команды

```bash
# Просмотр логов
vercel logs

# Просмотр переменных окружения
vercel env ls

# Удаление переменной
vercel env rm VARIABLE_NAME

# Просмотр деплоев
vercel ls
```

## 🎉 Готово!

После успешного деплоя ваш сайт будет доступен по адресу:
- Production: `https://your-project.vercel.app`
- Preview: `https://your-project-git-branch.vercel.app`
