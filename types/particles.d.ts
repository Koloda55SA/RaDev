declare module 'particles.js'

declare global {
  interface Window {
    particlesJS: (elementId: string, params: any) => void
  }
}

export {}


declare global {
  interface Window {
    particlesJS: (elementId: string, params: any) => void
  }
}

export {}

